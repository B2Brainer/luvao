generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model ScrapedProduct {
  id           String   @id @default(uuid())
  storeName    String
  query        String
  name         String
  price        Float?
  url          String?
  availability Boolean?
  scrapedAt    DateTime @default(now())

  @@index([storeName])
  @@index([query])
  @@index([name])
  @@index([scrapedAt])
  @@index([storeName, query])
  @@index([query, name])
  @@map("scraped_products")
}


